#!/bin/sh
set -eu

case "${1:-}" in
    configure)
        cat <<'EOF'
scanbus installed its user-level unit and D-Bus activation file.
Enable it per account, never globally:
  systemctl --user enable --now scanbus.service
EOF
        ;;
esac

exit 0

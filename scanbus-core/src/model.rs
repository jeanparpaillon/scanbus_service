//! Domain model: scanners, capabilities, buttons, pages.
//!
//! Filled in by issue 1.2. The types live here rather than being passed around as
//! `a{sv}` maps so that a typo in a key is a compile error instead of a scanner that
//! silently exposes zero buttons.
